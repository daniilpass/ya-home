import styled from '@emotion/styled';
import { Button } from '@mui/material';
import { LinkProps } from 'react-router-dom';

const RouterLinkButton = styled(Button)<LinkProps>({});

export default RouterLinkButton;