import HomeEditor from '../../widgets/HomeEditor';

import './style.css';

const EditorPage = () => {
    return (
        <div className="editor-page">
            <HomeEditor />
        </div>
    );
}

export default EditorPage;
